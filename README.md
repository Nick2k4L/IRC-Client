# IRC-Client
Creating an IRCClient to use for the desktop-router-console, having a pre-baked in IRC-Client would simplify things.

## Currently Does not work as intended since I am porting into a flutter application. I will eventually make a mirror for a bubble tea client. This is strictly the `backend` of the IRC client.
